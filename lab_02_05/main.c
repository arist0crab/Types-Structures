#include "status.h"
#include "data.h"
#include "process.h"
#include "print_instructions.h"
#include <stdbool.h>

#define MAX_PLAYS_QUANTITY 1000

static theater_play_t *theater_plays_arr;

int main(void)
{
    status_t exit_code = SUCCCESS_CODE;
    bool program_running = false;

    // проверяем что можем выделить память
    

    while (program_running && exit_code == SUCCCESS_CODE)
    {
        
    }
    

    return exit_code;
}