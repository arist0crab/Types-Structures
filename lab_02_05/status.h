#ifndef __STATUS_H__
#define __STATUS_H__

typedef enum 
{
    SUCCCESS_CODE,
    INVALID_QUANTITY,
    INVALID_POINTER,
    INVALID_INPUT,
    MEM_ALLOCATE_ERROR,
    INVALID_CHOICE
} status_t;

#endif